# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database (SQLite for dev)
sqlmodel==0.0.14
alembic==1.13.1
aiosqlite==0.20.0
# psycopg2-binary==2.9.9  # Uncomment when switching to PostgreSQL

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-dotenv==1.0.0
pydantic-settings==2.1.0

# Supabase (Auth only)
# 2.15.3+ uses httpx>=0.28 (compatible with google-genai==1.66.0)
supabase==2.15.3

# AI — Gemini 2.5 Flash (Google AI Studio free tier)
# Pinned to avoid slow pip backtracking during install
google-genai==1.66.0

# Image processing (for Gemini Vision multipart upload)
pillow==10.2.0

# Utilities
# httpx: NOT pinned here — let supabase/google-genai pick compatible version
# pydantic: use >= to allow google-genai to request newer versions
pydantic>=2.5.3
email-validator==2.1.0
python-dateutil==2.8.2

# Cache
redis==5.0.1

# RAG — in-memory vector similarity (no external vector DB needed)
numpy>=1.26.0

# Multi-provider LLM support (optional — uncomment based on LLM_PROVIDER)
# openai>=1.30.0       # pip install openai   — needed if LLM_PROVIDER=openai
# anthropic>=0.30.0    # pip install anthropic — needed if LLM_PROVIDER=anthropic

# MCP (Model Context Protocol) — tool orchestration + agentic layer
mcp>=1.0.0
fastmcp>=2.0.0

# Logging
loguru==0.7.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Dev tools
black==24.1.1
ruff==0.1.14
mypy==1.8.0
